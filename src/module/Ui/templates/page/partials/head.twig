{% do normalize().headMeta(revision).headTitle(revision) %}
<div class="page-header">
    {% set controls = include('page/partials/controls') %}
    {% do placeholder('controls').set(controls) %}
    <h1 itemprop="name">{{ revision.getTitle() }}</h1>
</div>
