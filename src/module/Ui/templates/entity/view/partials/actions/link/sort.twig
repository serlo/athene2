{% set options = entity().getOptions(entity) %}
{% if options.hasComponent('link') %}
    {% if isGranted('entity.link.order', entity) %}
        <li>
            <a href="{{ url('entity/link/order', {'entity': entity.getId(), 'type': 'link'}) }}">
                <span class="fa fa-sort"></span>
                {% trans %}
                    Order children
                {% endtrans %}
            </a>
        </li>
    {% endif %}
{% endif %}