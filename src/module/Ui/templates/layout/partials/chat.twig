{% if user().isUserLoggedIn() %}

    <script src="https://scrollback.io/s/sb.js"></script>
    <script>
        var widget = scrollback({
            form: 'toast',
            minimize: 'true',

            room: 'serlo',
            thread: 'all',

            nick: '{{ user().getAuthenticatedUserName()  }}'
        });
    </script>
{% endif %}