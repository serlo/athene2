{% set user = normalize().toAnchor(event.getActor()) %}
{% set discussion = normalize().toAnchor(event.getObject()) %}
{% set on = normalize().toAnchor(event.getParameter('on')) %}
{% trans %}
    {{ user }} started a new discussion {{ discussion }} on {{ on }}
{% endtrans %}