<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for object in objects if normalize().possible(object) %}
        <url>
            <loc>{{ normalize().toUrl(object, true) }}</loc>
        </url>
    {% endfor %}
</urlset>