{% if uuid %}
    {% set form = uuidForm().getForm('purge', uuid) %}
    {% do form.prepare() %}
    {{ form().openTag(form) }}
    {{ formRow(form.get('csrf')) }}
    {% if sendbutton %}
        {{ sendbutton }}
    {% else %}
        {{ formRow(form.get('submit')) }}
    {% endif %}
    {{ form().closeTag() }}
{% endif %}