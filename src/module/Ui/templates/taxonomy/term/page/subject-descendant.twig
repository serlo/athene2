{% set title = term.getName() %}
<div class="page-header">
    {% set controls = include('taxonomy/term/templates/partials/controls') %}
    {% do placeholder('controls').set(controls) %}
    {% do placeholder('breadcrumbs').set('<li><span>' ~ title ~ '</span></li>') %}
    {#
    {% if term.hasParent() %}
        <ul class="nav nav-pills pull-left">
            <li>
                <a href="{{ url('taxonomy/term/get', {'term': term.getParent().getId()}) }}">
                    <span class="fa fa-chevron-left"></span>
                </a>
            </li>
        </ul>
    {% endif %}
    #}
    <h1>{{ title }}</h1>
</div>
{% include 'taxonomy/term/page/partials/alerts' %}
{% include 'taxonomy/term/templates/default' %}
