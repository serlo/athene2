{% set headTitle = term.getName()%}
{% for taxonomyTerm in taxonomy().flattenAncestors(term) %}
    {% set headTitle = taxonomyTerm.getName() ~ '->' ~ headTitle %}
{% endfor %}
{% do headTitle(headTitle) %}

{% include 'taxonomy/term/page/subject-descendant' %}