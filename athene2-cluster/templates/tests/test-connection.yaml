apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "athene2-cluster.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "athene2-cluster.name" . }}
    helm.sh/chart: {{ include "athene2-cluster.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "athene2-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
