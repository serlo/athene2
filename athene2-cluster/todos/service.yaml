## Erstellt mit "helm create", bitte überarbeiten

apiVersion: v1
kind: Service
metadata:
  name: {{ include "athene2-cluster.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "athene2-cluster.name" . }}
    helm.sh/chart: {{ include "athene2-cluster.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "athene2-cluster.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
